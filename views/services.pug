<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
    body
        form(action="/services" method="post")
            input(type="text" name="services_target" placeholder="터치한 후 선택해주세요" list="servicesList" required="true")
            datalist(id="servicesList")
                option(value="사장님")
                option(value="결제해주시는 분")
                option(value="알바생")
            h3 에서 어떤 문제가 있었는지 한줄로 간단하게 적어주세요!
            textarea(name="services_message", cols="40", rows="2" required="true")
            input(type="submit", value="제출하고 상품 받아가세요!!")

	

